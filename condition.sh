#!/bin/bash

#[[ -a ${file} ]] - if file exsists
#[[ -b ${file} ]] - if file exsist and is a block specific file.
#[[ -c ${file} ]] - if file exsist and is a character special file
#[[ -d ${file} ]] - if file exsist and is a directory
#[[ -e ${file} ]] - if file exsist
#[[ -f ${file} ]] - if file exsist and is a regular file
#[[ -h ${file} ]] - if file exsist and is a symbolic link
#[[ -r ${file} ]] - if file exsist and is readable
#[[ -s ${file} ]] - if file exsist and size is greater than zero
#[[ -w ${file} ]] - if file exsist and is  writable
#[[ -x ${file} ]] - if file exsist and is executable
#[[ -L ${file} ]] - if file exsist and is symbolic link


#string operation
#[[ -v ${varname} ]]
#[[ -z ${string} ]]
#[[ -n ${string} ]]
